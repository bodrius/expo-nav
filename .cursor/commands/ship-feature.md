# Ship feature (code pipeline)

Requirements and plan are **already approved**. Run implementation + quality pipeline only.

## Your job

1. Use the **feature-pipeline** subagent in **ship** mode.
2. Pass:
   - Paths to `docs/requirements/<slug>.md` and `docs/plans/<slug>.md` (user should mention or @-reference them)
   - Any extra implementation notes from the user message
3. Do not re-run requirements-planner unless the user asks.
4. Do not create git commits unless the user explicitly asked.

## Context

<!-- User: @docs/requirements/... @docs/plans/... plus optional notes -->

If requirements or plan paths are missing, ask once before starting.
